# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/tests/product.api.spec.ts >> Product API >> should create a new product and return created data
- Location: src/api/tests/product.api.spec.ts:16:3

# Error details

```
TypeError: request.newContext is not a function
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { ProductClient } from '../clients/product.client.js';
  3  | 
  4  | const newProduct = {
  5  |   title: 'BMW Pencil',
  6  |   description: 'A premium writing instrument for aspiring engineers.',
  7  |   price: 9.99,
  8  |   discountPercentage: 5,
  9  |   rating: 4.5,
  10 |   stock: 120,
  11 |   brand: 'BMW',
  12 |   category: 'stationery'
  13 | };
  14 | 
  15 | test.describe('Product API', () => {
  16 |   test('should create a new product and return created data', async ({ request, baseURL }) => {
> 17 |     const api = await request.newContext({ baseURL: 'https://dummyjson.com' });
     |                               ^ TypeError: request.newContext is not a function
  18 |     const client = new ProductClient(api);
  19 | 
  20 |     const createResponse = await client.createProduct(newProduct);
  21 |     expect(createResponse.ok()).toBeTruthy();
  22 |     const created = await createResponse.json();
  23 |     expect(created.title).toBe(newProduct.title);
  24 |     expect(created.brand).toBe(newProduct.brand);
  25 | 
  26 |     const listResponse = await client.getProducts();
  27 |     expect(listResponse.ok()).toBeTruthy();
  28 |     const list = await listResponse.json();
  29 |     expect(list.products).toEqual(expect.arrayContaining([expect.objectContaining({ title: newProduct.title })]));
  30 |   });
  31 | });
  32 | 
```